# سجل التغييرات

جميع التغييرات الملحوظة في مشروع **ألوان السفر للسفر والسياحة**.

التنسيق مبني على [Keep a Changelog](https://keepachangelog.com/ar/1.1.0/).

---

## [غير منشور]

### قيد الإنجاز
- **دعم الإنجليزية**: الصفحة الرئيسية فقط مترجمة. باقي الصفحات العامة
  (تواصل معنا، التأشيرة الأمريكية، التأشيرات، تفاصيل التأشيرة، الرخصة الدولية،
  الشروط، الخصوصية) ونماذج التقديم ما زالت تعرض نصاً عربياً تحت `/en`.
- **SEO للنسخة الإنجليزية**: وسوم `meta` لصفحات `/en` ما زالت عربية،
  ووسوم `hreflang` وخريطة الموقع ثنائية اللغة غير مضافة.
- **حقول `En` في قاعدة البيانات**: المحتوى القابل للتحرير (الهيرو، الخدمات،
  الأسئلة الشائعة، التأشيرات) يحتاج حقولاً إنجليزية وخانات إدخال في لوحة التحكم.

---

## [2026-08-18]

### أُضيف
- **دعم اللغة الإنجليزية (الأساس)** — سياق لغة، قاموسا ترجمة، ومسارات `/en`
  لكل صفحات الموقع العام مع تبديل `lang`/`dir` تلقائياً. الصفحة الرئيسية مترجمة بالكامل.
- **شريط التوثيق الرسمي** في تذييل كل الصفحات العامة — شعار المركز السعودي
  للأعمال مع الرقم الوطني الموحد ورقم رخصة وزارة السياحة.
- **رقما التسجيل قابلان للتعديل** من لوحة التحكم ← الإعدادات ← الإعدادات العامة،
  ويصلان للموقع ضمن استجابة محتوى الموقع العام.
- **نافذة تفاصيل الموعد بعمودين** تُفتح بالنقر على صف الجدول، بنفس تخطيط
  نافذة المهام (`DetailModal.css` مشترك).
- **بحث العملاء على الخادم** في شاشة إضافة موعد بدل قائمة منسدلة محدودة.

### تغيّر
- **إعادة تسمية الوكالة** من «ألوان المسافر» إلى «ألوان السفر» في 143 موضعاً —
  الموقع العام، لوحة التحكم، بوابة العملاء، الفواتير والإيصالات، قوالب البريد،
  ونماذج قاعدة البيانات.
- **الهوية البصرية**: استبدال الأيقونات والشعارات، وتوليد `favicon.ico`
  متعدد المقاسات (16→256)، وتحديث `manifest.json` و`site.webmanifest`
  اللذين كانا على قيم Create React App الافتراضية.
- **صورة المشاركة الاجتماعية** صارت `og-1200x630.png` بدل الشعار المربّع
  الذي كان يُقصّ في واتساب وتويتر.
- **رقم التواصل** صار `+966558741741`، والضغط عليه في الموقع العام يفتح
  واتساب مباشرة بدل تطبيق الهاتف.
- **إعادة تصميم الهيدر والفوتر**: شريط علوي بالتواصل والترخيص ينطوي عند
  التمرير، وقائمة جوال كدرج جانبي مع طبقة تعتيم وقفل تمرير الخلفية.
- **توحيد نافذة تفاصيل المهمة** على ملف الأنماط المشترك (حُذفت 39 قاعدة مكرّرة).

### أُصلح
- **الشعار المرفوع لا يظهر في الإنتاج**: سبعة مواضع كانت تدمج
  `REACT_APP_API_URL` (المنتهي بـ `/api`) مباشرة مع مسار الملف فينتج
  `/api/uploads/...` بينما الخادم يقدّمها على `/uploads`. وُحّدت عبر `assetUrl`.
- **شعار التقارير المطبوعة** كان يبني الرابط من أصل الواجهة لا الخادم.
- **صفحة العملاء تعرض 50 عميلاً فقط** — لم تكن تمرّر معاملات الترقيم،
  فيطبّق الخادم حدّه الافتراضي. أثّر ذلك على القائمة وبطاقات الإحصائيات معاً.
- **مسارات `/en` تسقط على معالج 404** الذي يرجع JSON بدل صفحة — سُجّلت على الخادم.
- **رقم الهاتف في التذييل يظهر معكوساً** بسبب اتجاه النص ثنائي الاتجاه.
- **حالة الهيدر عند إعادة التحميل** على موضع مُمرَّر مسبقاً كانت تبقى متمددة.

### ملاحظات النشر
- شغّل `node server/src/scripts/renameCompany.js` مرة واحدة بعد `git pull`
  لتحديث الاسم في الوثائق الموجودة بقاعدة البيانات — القيم الافتراضية في
  الكود تنطبق عند الإنشاء الأول فقط.
- امسح كاش Cloudflare بعد النشر؛ الأصول الثابتة (خاصة `favicon.ico`)
  مخزّنة على الحافة بـ `max-age` شهر.

---

## [2026-08-17]

### تغيّر
- إعادة تصميم نموذج الفاتورة الجديدة، مع حفظ تلقائي للمسودة وإجراءات بعد الإنشاء.

## [2026-06-24]

### أُضيف
- تنقّل متجاوب للجوال في لوحة التحكم.

## [2026-06-18]

### أُضيف
- حقوق «برمجة وتطوير محمد صالح» في التذييل مع رابط واتساب.

### أُصلح
- تثبيت تذييل نموذج الفاتورة، وإضافة باركود Code39 لملف الفاتورة والطباعة.
